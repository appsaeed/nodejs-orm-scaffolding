import { env } from "../app/base/helpers";
import session from "./session";


export default {

  //applications rest session configuration
  session,

  /**
   * ==================================================================
   * Application name 
   * ==================================================================
   */
  name: env("APP_NAME", "Nodejs"),

  /**
   * ==================================================================
   * Application title 
   * ==================================================================
   */
  title: env("APP_TITLE", "Nodejs express app like laravel"),

  /**
   * ==================================================================
   * Application kaywords 
   * ==================================================================
   */
  keyword: env("APP_KEYWORD", "Nodejs, express, laravel, framework"),

  /**
   * ==================================================================
   * Application logo 
   * ==================================================================
   */
  logo: env("APP_LOGO", ""),

  /**
   * ==================================================================
   * Application favicon 
   * ==================================================================
   */
  favicon: env("APP_FAVICON", ""),

  /**
   * ==================================================================
   * Application env 
   * ==================================================================
   */
  env: env("APP_ENV", "production"),

  /**
   * ==================================================================
   * Application stage 
   * ==================================================================
   */
  stage: env("APP_STAGE", "Live"),

  /**
   * ==================================================================
   * Application debug 
   * ==================================================================
   */
  debug: Boolean(env("APP_DEBUG", false)),

  /**
   * ==================================================================
   * Application url 
   * ==================================================================
   */
  url: env("APP_URL", "http://localhost"),

  /**
   * ==================================================================
   * Application timezone 
   * ==================================================================
   */
  timezone: env("APP_TIMEZONE", "UTC"),

  /**
   * ==================================================================
   * Application date format 
   * ==================================================================
   */
  date_format: env("APP_DATE_FORMAT", "jS M y"),

  /**
   * ==================================================================
   * Application locale 
   * ==================================================================
   */
  locale: env("APP_LOCALE", "en"),

  /**
   * ==================================================================
   * Application key 
   * ==================================================================
   */
  key: env("APP_KEY"),

  cipher: "AES-256-CBC",

  /**
   * ==================================================================
   * Application country 
   * ==================================================================
   */
  country: env("APP_COUNTRY", "Bangladesh"),

  /**
   * ==================================================================
   * Application view engine 
   * ==================================================================
   */
  view_engine: env("VIEW_ENGINE", 'ejs'),

  /**
   * ==================================================================
   * Application view engine 
   * ==================================================================
   */
  view_path: env("VIEW_ENGINE_PATH", 'views'),

  /**
   * ==================================================================
   * Application force https 
   * ==================================================================
   */
  url_force_https: Boolean(env("URL_FORCE_HTTPS", false)),

  /**
   * ==================================================================
   * Application two factor authentication
   * ==================================================================
   */
  two_factor: Boolean(env("TWO_FACTOR", false))
};
